# Archived duplicate summaries

This directory contains summary-only directories produced under an obsolete
`*_comparison` default name. Their numerical CSV and figure artifacts were
verified byte-for-byte against the canonical directories before archival.

Canonical location:

```text
runs/coefficient_bootstrap/xz_inherit_xonly_tuning_b2000/
outputs/coefficient_bootstrap/xz_inherit_xonly_tuning_b2000/
```

No bootstrap replicate checkpoints are stored here, and no computational
results were deleted during this cleanup.
