# Archived Python IID VCTR

This directory preserves the previous Python port of the iid VCTR baseline.

- Source basis: `code_and_data/`
- Method reference: `VaryingCoefLM.pdf`
- Status: archived, not the active development line

The active Python development line now lives under `src/` and targets the paired-eye VCTR model described by `VaryingCoefPLM.pdf`.
